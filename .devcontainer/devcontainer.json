{
    "name": "Python Codespace",
    "build": {
      "dockerfile": "Dockerfile"
    },
    "customizations": {
      "vscode": {
        "extensions": ["ms-python.python"]
      }
    },
    "postCreateCommand": "pip install -r requirements.txt",  // Optional: Install dependencies
    "postStartCommand": "python3 demo-app.py"                // Run your Python file
  }
  